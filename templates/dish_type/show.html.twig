{% extends 'base.html.twig' %}

{% block title %}DishType{% endblock %}

{% block body %}
    <h1>DishType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dish_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ dish_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dish_type_index') }}">back to list</a>

    <a href="{{ path('app_dish_type_edit', {'id': dish_type.id}) }}">edit</a>

    {{ include('dish_type/_delete_form.html.twig') }}
{% endblock %}
